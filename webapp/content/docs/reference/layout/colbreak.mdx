---
title: "Column Break"
description: "Documentation for the `colbreak` function."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Forces a column break.

The function will behave like a [page break](/docs/reference/layout/pagebreak/) when used in a single column layout or the last column on a page. Otherwise, content after the column break will be placed in the next column.

## Example

<TypstPreview
  code={`
#set page(columns: 2)
Preliminary findings from our
ongoing research project have
revealed a hitherto unknown
phenomenon of extraordinary
significance.

#colbreak()
Through rigorous experimentation
and analysis, we have discovered
a hitherto uncharacterized process
that defies our current
understanding of the fundamental
laws of nature.
`}
  image='/assets/317ca576aa5033b3292e2f600bab0f18.png'
  alt='Preview'
/>

```typst
#colbreak(
  weak: bool
) -> content
```
## Parameters

<TypeTable
  type={{
    'weak': {
      'description': 'If `true{:typst}`, the column break is skipped if the current column is already empty.',
      'type': 'bool',
      'default': '<code><span class="typ-key">false</span></code>'
    }
  }}
/>


