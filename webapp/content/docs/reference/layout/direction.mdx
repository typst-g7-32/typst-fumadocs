---
title: "Direction"
description: "Documentation for the Direction type."
---
import { TypeTable } from 'fumadocs-ui/components/type-table';

The four directions into which content can be laid out.

Possible values are:

- `ltr{:typst}`: Left to right.
- `rtl{:typst}`: Right to left.
- `ttb{:typst}`: Top to bottom.
- `btt{:typst}`: Bottom to top.

These values are available globally and also in the direction type's scope, so you can write either of the following two:

```typst
#stack(dir: rtl)[A][B][C]
#stack(dir: direction.rtl)[A][B][C]
```


## Methods

### `from`

Returns a direction from a starting point.

```typst
#direction.from(left) \
#direction.from(right) \
#direction.from(top) \
#direction.from(bottom)
```

```#direction.from(side) -> direction```

<TypeTable
  type={{
    'side': {
      'description': '',
      'type': 'alignment',
      'default': 'None'
    }
  }}
/>


### `to`

Returns a direction from an end point.

```typst
#direction.to(left) \
#direction.to(right) \
#direction.to(top) \
#direction.to(bottom)
```

```#direction.to(side) -> direction```

<TypeTable
  type={{
    'side': {
      'description': '',
      'type': 'alignment',
      'default': 'None'
    }
  }}
/>


### `axis`

The axis this direction belongs to, either `"horizontal"{:typst}` or `"vertical"{:typst}`.

```typst
#ltr.axis() \
#ttb.axis()
```

```#direction.axis() -> str```

### `sign`

The corresponding sign, for use in calculations.

```typst
#ltr.sign() \
#rtl.sign() \
#ttb.sign() \
#btt.sign()
```

```#direction.sign() -> int```

### `start`

The start point of this direction, as an alignment.

```typst
#ltr.start() \
#rtl.start() \
#ttb.start() \
#btt.start()
```

```#direction.start() -> alignment```

### `end`

The end point of this direction, as an alignment.

```typst
#ltr.end() \
#rtl.end() \
#ttb.end() \
#btt.end()
```

```#direction.end() -> alignment```

### `inv`

The inverse direction.

```typst
#ltr.inv() \
#rtl.inv() \
#ttb.inv() \
#btt.inv()
```

```#direction.inv() -> direction```

