---
title: "Lowercase"
description: "Documentation for the `lower` function."
---
import { TypeTable } from 'fumadocs-ui/components/type-table';


## `lower`

Converts a string or content to lowercase.

## Example

```typst
#lower("ABC") \
#lower[*My Text*] \
#lower[already low]
```

```#lower(text) -> str content```

<TypeTable
  type={{
    'text': {
      'description': 'The text to convert to lowercase.',
      'type': 'str | content',
      'default': 'None'
    }
  }}
/>


