---
title: "Stroke"
description: "Documentation for the Stroke type."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Defines how to draw a line.

A stroke has a _paint_ (a solid color or gradient), a _thickness,_ a line _cap,_ a line _join,_ a _miter limit,_ and a _dash_ pattern. All of these values are optional and have sensible defaults.

## Example

<TypstPreview
  code={`
#set line(length: 100%)
#stack(
  spacing: 1em,
  line(stroke: 2pt + red),
  line(stroke: (paint: blue, thickness: 4pt, cap: "round")),
  line(stroke: (paint: blue, thickness: 1pt, dash: "dashed")),
  line(stroke: 2pt + gradient.linear(..color.map.rainbow)),
)
`}
  image='/assets/dcda1fb9b6f022596876c15ac2535df3.png'
  alt='Preview'
/>

## Simple strokes

You can create a simple solid stroke from a color, a thickness, or a combination of the two. Specifically, wherever a stroke is expected you can pass any of the following values:

- A length specifying the stroke's thickness. The color is inherited, defaulting to black.
- A color to use for the stroke. The thickness is inherited, defaulting to `1pt{:typst}`.
- A stroke combined from color and thickness using the `+{:typst}` operator as in `2pt + red{:typst}`.

For full control, you can also provide a [dictionary](/docs/reference/foundations/dictionary/) or a `stroke{:typst}` object to any function that expects a stroke. The dictionary's keys may include any of the parameters for the constructor function, shown below.

## Fields

On a stroke object, you can access any of the fields listed in the constructor function. For example, `(2pt + blue).thickness{:typst}` is `2pt{:typst}`. Meanwhile, `stroke(red).cap{:typst}` is `auto{:typst}` because it's unspecified. Fields set to `auto{:typst}` are inherited.

## Constructor
Converts a value to a stroke or constructs a stroke with the given parameters.

Note that in most cases you do not need to convert values to strokes in order to use them, as they will be converted automatically. However, this constructor can be useful to ensure a value has all the fields of a stroke.

<TypstPreview
  code={`
#let my-func(x) = {
    x = stroke(x) // Convert to a stroke
    [Stroke has thickness #x.thickness.]
}
#my-func(3pt) 
#my-func(red) 
#my-func(stroke(cap: "round", thickness: 1pt))
`}
  image='/assets/a2e95c5c335ca6e9c24b156f08f5cc25.png'
  alt='Preview'
/>

```typst
#stroke(
  paint,
  thickness,
  cap,
  join,
  dash,
  miter-limit
) -> stroke
```
### Parameters

<TypeTable
  type={{
    'paint': {
      'description': 'The color or gradient to use for the stroke.  If set to `auto{:typst}`, the value is inherited, defaulting to `black{:typst}`.',
      'type': 'auto | color | gradient | tiling',
      'default': 'None'
    },
    'thickness': {
      'description': 'The stroke\'s thickness.  If set to `auto{:typst}`, the value is inherited, defaulting to `1pt{:typst}`.',
      'type': 'auto | length',
      'default': 'None'
    },
    'cap': {
      'description': 'How the ends of the stroke are rendered.  If set to `auto{:typst}`, the value is inherited, defaulting to `"butt"{:typst}`.',
      'type': 'auto | str',
      'default': 'None'
    },
    'join': {
      'description': 'How sharp turns are rendered.  If set to `auto{:typst}`, the value is inherited, defaulting to `"miter"{:typst}`.',
      'type': 'auto | str',
      'default': 'None'
    },
    'dash': {
      'description': 'The dash pattern to use. This can be:  - One of the predefined patterns:    - `"solid"{:typst}` or `none{:typst}`   - `"dotted"{:typst}`   - `"densely-dotted"{:typst}`   - `"loosely-dotted"{:typst}`   - `"dashed"{:typst}`   - `"densely-dashed"{:typst}`   - `"loosely-dashed"{:typst}`   - `"dash-dotted"{:typst}`   - `"densely-dash-dotted"{:typst}`   - `"loosely-dash-dotted"{:typst}` - An [array](/docs/reference/foundations/array/) with alternating lengths for dashes and gaps. You can also use the string `"dot"{:typst}` for a length equal to the line thickness. - A [dictionary](/docs/reference/foundations/dictionary/) with the keys `array{:typst}` (same as the array above), and `phase{:typst}` (of type [length](/docs/reference/layout/length/)), which defines where in the pattern to start drawing.  If set to `auto{:typst}`, the value is inherited, defaulting to `none{:typst}`.  <TypstPreview   code={` #set line(length: 100%, stroke: 2pt) #stack(   spacing: 1em,   line(stroke: (dash: "dashed")),   line(stroke: (dash: (10pt, 5pt, "dot", 5pt))),   line(stroke: (dash: (array: (10pt, 5pt, "dot", 5pt), phase: 10pt))), ) `}   image=\'/assets/3f7f20165b8a65cc3ae1675947ce671e.png\'   alt=\'Preview\' />',
      'type': 'none | auto | str | array | dictionary',
      'default': 'None'
    },
    'miter-limit': {
      'description': 'Number at which protruding sharp bends are rendered with a bevel instead or a miter join. The higher the number, the sharper an angle can be before it is bevelled. Only applicable if `join{:typst}` is `"miter"{:typst}`.  Specifically, the miter limit is the maximum ratio between the corner\'s protrusion length and the stroke\'s thickness.  If set to `auto{:typst}`, the value is inherited, defaulting to `4.0{:typst}`.  <TypstPreview   code={` #let items = (   curve.move((15pt, 0pt)),   curve.line((0pt, 30pt)),   curve.line((30pt, 30pt)),   curve.line((10pt, 20pt)), )  #set curve(stroke: 6pt + blue) #stack(   dir: ltr,   spacing: 1cm,   curve(stroke: (miter-limit: 1), ..items),   curve(stroke: (miter-limit: 4), ..items),   curve(stroke: (miter-limit: 5), ..items), ) `}   image=\'/assets/33818a9431604770d2115dd8e445c269.png\'   alt=\'Preview\' />',
      'type': 'auto | float',
      'default': 'None'
    }
  }}
/>


