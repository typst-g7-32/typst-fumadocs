---
title: "Круг"
description: "Документация для функции `circle`."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Круг с необязательным содержимым.

## Пример

<TypstPreview
  code={`
  // Без содержимого.
  #circle(radius: 25pt)

  // С содержимым.
  #circle[
    #set align(center + horizon)
    Автоматически \\
    подгоняется под размер.
  ]
`}
  image='/typst-fumadocs/assets/1f59e2c057a82944d5833baa72667f56.png'
  alt='Предварительный просмотр'
  editable={true}
/>

```typst
#circle(
  radius: length,
  width: auto | relative,
  height: auto | relative | fraction,
  fill: none | color | gradient | tiling,
  stroke: none | auto | length | color | gradient | stroke | tiling | dictionary,
  inset: relative | dictionary,
  outset: relative | dictionary,
  body
) -> content
```
## Параметры

<TypeTable
  type={{
    'radius': {
      'description': 'Радиус круга. Взаимоисключающий параметр с `width{:typst}` и `height{:typst}`.',
      'type': 'length',
      'default': '<code><span class="typ-num">0pt</span></code>'
    },
    'width': {
      'description': 'Ширина круга. Взаимоисключающий параметр с `radius{:typst}` и `height{:typst}`.  В отличие от `radius{:typst}`, может быть относительным к ширине родительского контейнера.',
      'type': 'auto | relative',
      'default': '<code><span class="typ-key">auto</span></code>'
    },
    'height': {
      'description': 'Высота круга. Взаимоисключающий параметр с `radius{:typst}` и `width{:typst}`.  В отличие от `radius{:typst}`, может быть относительным к высоте родительского контейнера.',
      'type': 'auto | relative | fraction',
      'default': '<code><span class="typ-key">auto</span></code>'
    },
    'fill': {
      'description': 'Как заполнить круг. См. [документацию прямоугольника](/docs/reference/visualize/rect/#parameters-fill) для подробностей.',
      'type': 'none | color | gradient | tiling',
      'default': '<code><span class="typ-key">none</span></code>'
    },
    'stroke': {
      'description': 'Как обвести круг. См. [документацию прямоугольника](/docs/reference/visualize/rect/#parameters-stroke) для подробностей.',
      'type': 'none | auto | length | color | gradient | stroke | tiling | dictionary',
      'default': '<code><span class="typ-key">auto</span></code>'
    },
    'inset': {
      'description': 'Внутренние отступы содержимого круга. См. [документацию box](/docs/reference/layout/box/#parameters-inset) для подробностей.',
      'type': 'relative | dictionary',
      'default': '<code><span class="typ-num">0%</span> <span class="typ-op">+</span> <span class="typ-num">5pt</span></code>'
    },
    'outset': {
      'description': 'Насколько расширить размер круга без влияния на макет. См. [документацию box](/docs/reference/layout/box/#parameters-outset) для подробностей.',
      'type': 'relative | dictionary',
      'default': '<code><span class="typ-punct">(</span><span class="typ-punct">:</span><span class="typ-punct">)</span></code>'
    },
    'body': {
      'description': 'Содержимое, помещаемое в круг. Круг расширяется, чтобы вместить это содержимое, сохраняя соотношение сторон 1:1.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>
