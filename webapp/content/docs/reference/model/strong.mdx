---
title: "Strong Emphasis"
description: "Documentation for the `strong` function."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Strongly emphasizes content by increasing the font weight.

Increases the current font weight by a given `delta{:typst}`.

## Example

<TypstPreview
  code={`
  This is *strong.* \
  This is #strong[too.] \

  #show strong: set text(red)
  And this is *evermore.*
`}
  image='/typst-fumadocs/assets/f0f155e1250d5cd6db61ef6e1d6d484c'
  alt='Preview'
  editable={true}
/>

## Syntax

This function also has dedicated syntax: To strongly emphasize content, simply enclose it in stars/asterisks (`*{:typst}`). Note that this only works at word boundaries. To strongly emphasize part of a word, you have to use the function.

```typst
#strong(
  delta: int,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'delta': {
      'description': 'The delta to apply on the font weight.  <TypstPreview   code={`   #set strong(delta: 0)   No *effect!* `}   image=\'/typst-fumadocs/assets/482ecb9a7454c6daefc502f7df57e97c\'   alt=\'Preview\'   editable={true} />',
      'type': 'int',
      'default': '<code><span class="typ-num">300</span></code>'
    },
    'body': {
      'description': 'The content to strongly emphasize.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>


