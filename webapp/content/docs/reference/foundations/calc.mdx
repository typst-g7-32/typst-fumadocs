---
title: "Calculation"
description: "Documentation for the calc functions."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Module for calculations and processing of numeric values.

These definitions are part of the `calc{:typst}` module and not imported by default. In addition to the functions listed below, the `calc{:typst}` module also defines the constants `pi{:typst}`, `tau{:typst}`, `e{:typst}`, and `inf{:typst}`.

Calculates the absolute value of a numeric value.

<TypstPreview
  code={`
  #calc.abs(-5) \
  #calc.abs(5pt - 2cm) \
  #calc.abs(2fr) \
  #calc.abs(decimal("-342.440"))
`}
  image='/typst-fumadocs/assets/d673cd93e440c975041eb02ccf20a752.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.abs(
  value
) -> any
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The value whose absolute value to calculate.',
      'type': 'int | float | length | angle | ratio | fraction | decimal',
      'default': 'None'
    }
  }}
/>

Raises a value to some exponent.

<TypstPreview
  code={`
  #calc.pow(2, 3) \
  #calc.pow(decimal("2.5"), 2)
`}
  image='/typst-fumadocs/assets/610a0eb053713c4816d1bfa7f41fd5ac.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.pow(
  base,
  exponent
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'base': {
      'description': 'The base of the power.  If this is a [`decimal{:typst}`](/docs/reference/foundations/decimal/), the exponent can only be an [integer](/docs/reference/foundations/int/).',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'exponent': {
      'description': 'The exponent of the power.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Raises a value to some exponent of e.

<TypstPreview
  code={`
  #calc.exp(1)
`}
  image='/typst-fumadocs/assets/f78e20399a0a10231e8c567e0ecb8cf.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.exp(
  exponent
) -> float
```
## Parameters

<TypeTable
  type={{
    'exponent': {
      'description': 'The exponent of the power.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the square root of a number.

<TypstPreview
  code={`
  #calc.sqrt(16) \
  #calc.sqrt(2.5)
`}
  image='/typst-fumadocs/assets/ad28f3d5b5a490a62ac665a3c5ecc54f.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.sqrt(
  value
) -> float
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number whose square root to calculate. Must be non-negative.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the real nth root of a number.

If the number is negative, then n must be odd.

<TypstPreview
  code={`
  #calc.root(16.0, 4) \
  #calc.root(27.0, 3)
`}
  image='/typst-fumadocs/assets/837af196aa131a0a028cbb6213b6dc28.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.root(
  radicand,
  index
) -> float
```
## Parameters

<TypeTable
  type={{
    'radicand': {
      'description': 'The expression to take the root of.',
      'type': 'float',
      'default': 'None'
    },
    'index': {
      'description': 'Which root of the radicand to take.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates the sine of an angle.

When called with an integer or a float, they will be interpreted as radians.

<TypstPreview
  code={`
  #calc.sin(1.5) \
  #calc.sin(90deg)
`}
  image='/typst-fumadocs/assets/d1cfe7fae09be11ecb26e1681210bd9.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.sin(
  angle
) -> float
```
## Parameters

<TypeTable
  type={{
    'angle': {
      'description': 'The angle whose sine to calculate.',
      'type': 'int | float | angle',
      'default': 'None'
    }
  }}
/>

Calculates the cosine of an angle.

When called with an integer or a float, they will be interpreted as radians.

<TypstPreview
  code={`
  #calc.cos(1.5) \
  #calc.cos(90deg)
`}
  image='/typst-fumadocs/assets/45079cea074917942f470664bc8fb9d2.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.cos(
  angle
) -> float
```
## Parameters

<TypeTable
  type={{
    'angle': {
      'description': 'The angle whose cosine to calculate.',
      'type': 'int | float | angle',
      'default': 'None'
    }
  }}
/>

Calculates the tangent of an angle.

When called with an integer or a float, they will be interpreted as radians.

<TypstPreview
  code={`
  #calc.tan(1.5) \
  #calc.tan(90deg)
`}
  image='/typst-fumadocs/assets/32ee947cdff8e3ae0a261cbbf30be9ff.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.tan(
  angle
) -> float
```
## Parameters

<TypeTable
  type={{
    'angle': {
      'description': 'The angle whose tangent to calculate.',
      'type': 'int | float | angle',
      'default': 'None'
    }
  }}
/>

Calculates the arcsine of a number.

<TypstPreview
  code={`
  #calc.asin(0) \
  #calc.asin(1)
`}
  image='/typst-fumadocs/assets/47e7cfd9bb0aa9e93a0ab1d1c6c9a5bd.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.asin(
  value
) -> angle
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number whose arcsine to calculate. Must be between -1 and 1.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the arccosine of a number.

<TypstPreview
  code={`
  #calc.acos(0) \
  #calc.acos(1)
`}
  image='/typst-fumadocs/assets/df8b6fb603d1c7d65bd28150b5d9049e.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.acos(
  value
) -> angle
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number whose arccosine to calculate. Must be between -1 and 1.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the arctangent of a number.

<TypstPreview
  code={`
  #calc.atan(0) \
  #calc.atan(1)
`}
  image='/typst-fumadocs/assets/2ace6207833059734055779232284324.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.atan(
  value
) -> angle
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number whose arctangent to calculate.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the four-quadrant arctangent of a coordinate.

The arguments are `(x, y){:typst}`, not `(y, x){:typst}`.

<TypstPreview
  code={`
  #calc.atan2(1, 1) \
  #calc.atan2(-2, -3)
`}
  image='/typst-fumadocs/assets/4773e07ed6084d1b122d805e42a29edf.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.atan2(
  x,
  y
) -> angle
```
## Parameters

<TypeTable
  type={{
    'x': {
      'description': 'The X coordinate.',
      'type': 'int | float',
      'default': 'None'
    },
    'y': {
      'description': 'The Y coordinate.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the hyperbolic sine of a hyperbolic angle.

<TypstPreview
  code={`
  #calc.sinh(0) \
  #calc.sinh(1.5)
`}
  image='/typst-fumadocs/assets/4a2ecb56bdb6d32fb28ebe9fac3e6661.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.sinh(
  value
) -> float
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The hyperbolic angle whose hyperbolic sine to calculate.',
      'type': 'float',
      'default': 'None'
    }
  }}
/>

Calculates the hyperbolic cosine of a hyperbolic angle.

<TypstPreview
  code={`
  #calc.cosh(0) \
  #calc.cosh(1.5)
`}
  image='/typst-fumadocs/assets/56eb7fba31d6f1e9c901d388f79bfa6e.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.cosh(
  value
) -> float
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The hyperbolic angle whose hyperbolic cosine to calculate.',
      'type': 'float',
      'default': 'None'
    }
  }}
/>

Calculates the hyperbolic tangent of a hyperbolic angle.

<TypstPreview
  code={`
  #calc.tanh(0) \
  #calc.tanh(1.5)
`}
  image='/typst-fumadocs/assets/f289872963045e1f65b5cb16a66e110d.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.tanh(
  value
) -> float
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The hyperbolic angle whose hyperbolic tangent to calculate.',
      'type': 'float',
      'default': 'None'
    }
  }}
/>

Calculates the logarithm of a number.

If the base is not specified, the logarithm is calculated in base 10.

<TypstPreview
  code={`
  #calc.log(100)
`}
  image='/typst-fumadocs/assets/e2d7be7cfdc41587fd0857d74cd78b6e.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.log(
  value,
  base: float
) -> float
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number whose logarithm to calculate. Must be strictly positive.',
      'type': 'int | float',
      'default': 'None'
    },
    'base': {
      'description': 'The base of the logarithm. May not be zero.',
      'type': 'float',
      'default': '<code><span class="typ-num">10.0</span></code>'
    }
  }}
/>

Calculates the natural logarithm of a number.

<TypstPreview
  code={`
  #calc.ln(calc.e)
`}
  image='/typst-fumadocs/assets/6a1320737d2e55a5cc749c787fd6fbea.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.ln(
  value
) -> float
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number whose logarithm to calculate. Must be strictly positive.',
      'type': 'int | float',
      'default': 'None'
    }
  }}
/>

Calculates the factorial of a number.

<TypstPreview
  code={`
  #calc.fact(5)
`}
  image='/typst-fumadocs/assets/1f1d2fc9d5edb4d45425b6dd0d2bc697.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.fact(
  number
) -> int
```
## Parameters

<TypeTable
  type={{
    'number': {
      'description': 'The number whose factorial to calculate. Must be non-negative.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates a permutation.

Returns the `k{:typst}`-permutation of `n{:typst}`, or the number of ways to choose `k{:typst}` items from a set of `n{:typst}` with regard to order.

<TypstPreview
  code={`
  $ "perm"(n, k) &= n!/((n - k)!) \
    "perm"(5, 3) &= #calc.perm(5, 3) $
`}
  image='/typst-fumadocs/assets/ee601fe2c3e685eeab28acda98113e88.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.perm(
  base,
  numbers
) -> int
```
## Parameters

<TypeTable
  type={{
    'base': {
      'description': 'The base number. Must be non-negative.',
      'type': 'int',
      'default': 'None'
    },
    'numbers': {
      'description': 'The number of permutations. Must be non-negative.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates a binomial coefficient.

Returns the `k{:typst}`-combination of `n{:typst}`, or the number of ways to choose `k{:typst}` items from a set of `n{:typst}` without regard to order.

<TypstPreview
  code={`
  #calc.binom(10, 5)
`}
  image='/typst-fumadocs/assets/ddebd0735304e1e42a6f35e1ae627994.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.binom(
  n,
  k
) -> int
```
## Parameters

<TypeTable
  type={{
    'n': {
      'description': 'The upper coefficient. Must be non-negative.',
      'type': 'int',
      'default': 'None'
    },
    'k': {
      'description': 'The lower coefficient. Must be non-negative.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates the greatest common divisor of two integers.

This will error if the result of integer division would be larger than the maximum 64-bit signed integer.

<TypstPreview
  code={`
  #calc.gcd(7, 42)
`}
  image='/typst-fumadocs/assets/a8e2304325e90a7ad290e3403a720380.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.gcd(
  a,
  b
) -> int
```
## Parameters

<TypeTable
  type={{
    'a': {
      'description': 'The first integer.',
      'type': 'int',
      'default': 'None'
    },
    'b': {
      'description': 'The second integer.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates the least common multiple of two integers.

<TypstPreview
  code={`
  #calc.lcm(96, 13)
`}
  image='/typst-fumadocs/assets/6c499406e76ffdf79446f73811ba6b8.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.lcm(
  a,
  b
) -> int
```
## Parameters

<TypeTable
  type={{
    'a': {
      'description': 'The first integer.',
      'type': 'int',
      'default': 'None'
    },
    'b': {
      'description': 'The second integer.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Rounds a number down to the nearest integer.

If the number is already an integer, it is returned unchanged.

Note that this function will always return an [integer](/docs/reference/foundations/int/), and will error if the resulting [`float{:typst}`](/docs/reference/foundations/float/) or [`decimal{:typst}`](/docs/reference/foundations/decimal/) is larger than the maximum 64-bit signed integer or smaller than the minimum for that type.

<TypstPreview
  code={`
  #calc.floor(500.1)
  #assert(calc.floor(3) == 3)
  #assert(calc.floor(3.14) == 3)
  #assert(calc.floor(decimal("-3.14")) == -4)
`}
  image='/typst-fumadocs/assets/de93166c89228f4f7046079b0f8dd542.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.floor(
  value
) -> int
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to round down.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Rounds a number up to the nearest integer.

If the number is already an integer, it is returned unchanged.

Note that this function will always return an [integer](/docs/reference/foundations/int/), and will error if the resulting [`float{:typst}`](/docs/reference/foundations/float/) or [`decimal{:typst}`](/docs/reference/foundations/decimal/) is larger than the maximum 64-bit signed integer or smaller than the minimum for that type.

<TypstPreview
  code={`
  #calc.ceil(500.1)
  #assert(calc.ceil(3) == 3)
  #assert(calc.ceil(3.14) == 4)
  #assert(calc.ceil(decimal("-3.14")) == -3)
`}
  image='/typst-fumadocs/assets/5d517a01bc439d7c26ada18df8487532.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.ceil(
  value
) -> int
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to round up.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Returns the integer part of a number.

If the number is already an integer, it is returned unchanged.

Note that this function will always return an [integer](/docs/reference/foundations/int/), and will error if the resulting [`float{:typst}`](/docs/reference/foundations/float/) or [`decimal{:typst}`](/docs/reference/foundations/decimal/) is larger than the maximum 64-bit signed integer or smaller than the minimum for that type.

<TypstPreview
  code={`
  #calc.trunc(15.9)
  #assert(calc.trunc(3) == 3)
  #assert(calc.trunc(-3.7) == -3)
  #assert(calc.trunc(decimal("8493.12949582390")) == 8493)
`}
  image='/typst-fumadocs/assets/d0049da245a68400b1a7771b741cd28b.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.trunc(
  value
) -> int
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to truncate.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Returns the fractional part of a number.

If the number is an integer, returns `0{:typst}`.

<TypstPreview
  code={`
  #calc.fract(-3.1)
  #assert(calc.fract(3) == 0)
  #assert(calc.fract(decimal("234.23949211")) == decimal("0.23949211"))
`}
  image='/typst-fumadocs/assets/dd31885a1d8c1061480c007c0b59c440.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.fract(
  value
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to truncate.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Rounds a number to the nearest integer.

Half-integers are rounded away from zero.

Optionally, a number of decimal places can be specified. If negative, its absolute value will indicate the amount of significant integer digits to remove before the decimal point.

Note that this function will return the same type as the operand. That is, applying `round{:typst}` to a [`float{:typst}`](/docs/reference/foundations/float/) will return a `float{:typst}`, and to a [`decimal{:typst}`](/docs/reference/foundations/decimal/), another `decimal{:typst}`. You may explicitly convert the output of this function to an integer with [`int{:typst}`](/docs/reference/foundations/int/), but note that such a conversion will error if the `float{:typst}` or `decimal{:typst}` is larger than the maximum 64-bit signed integer or smaller than the minimum integer.

In addition, this function can error if there is an attempt to round beyond the maximum or minimum integer or `decimal{:typst}`. If the number is a `float{:typst}`, such an attempt will cause `float.inf{:typst}` or `-float.inf{:typst}` to be returned for maximum and minimum respectively.

<TypstPreview
  code={`
  #calc.round(3.1415, digits: 2)
  #assert(calc.round(3) == 3)
  #assert(calc.round(3.14) == 3)
  #assert(calc.round(3.5) == 4.0)
  #assert(calc.round(3333.45, digits: -2) == 3300.0)
  #assert(calc.round(-48953.45, digits: -3) == -49000.0)
  #assert(calc.round(3333, digits: -2) == 3300)
  #assert(calc.round(-48953, digits: -3) == -49000)
  #assert(calc.round(decimal("-6.5")) == decimal("-7"))
  #assert(calc.round(decimal("7.123456789"), digits: 6) == decimal("7.123457"))
  #assert(calc.round(decimal("3333.45"), digits: -2) == decimal("3300"))
  #assert(calc.round(decimal("-48953.45"), digits: -3) == decimal("-49000"))
`}
  image='/typst-fumadocs/assets/4b67d730d70fca54eaeaec25ed9469a0.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.round(
  value,
  digits: int
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to round.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'digits': {
      'description': 'If positive, the number of decimal places.  If negative, the number of significant integer digits that should be removed before the decimal point.',
      'type': 'int',
      'default': '<code><span class="typ-num">0</span></code>'
    }
  }}
/>

Clamps a number between a minimum and maximum value.

<TypstPreview
  code={`
  #calc.clamp(5, 0, 4)
  #assert(calc.clamp(5, 0, 10) == 5)
  #assert(calc.clamp(5, 6, 10) == 6)
  #assert(calc.clamp(decimal("5.45"), 2, decimal("45.9")) == decimal("5.45"))
  #assert(calc.clamp(decimal("5.45"), decimal("6.75"), 12) == decimal("6.75"))
`}
  image='/typst-fumadocs/assets/213edda085367c7d5425fd04fd23dce9.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.clamp(
  value,
  min,
  max
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to clamp.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'min': {
      'description': 'The inclusive minimum value.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'max': {
      'description': 'The inclusive maximum value.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Determines the minimum of a sequence of values.

<TypstPreview
  code={`
  #calc.min(1, -3, -5, 20, 3, 6) \
  #calc.min("typst", "is", "cool")
`}
  image='/typst-fumadocs/assets/69f392ae374e01cff5473cd4da1c5422.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.min(
  values
) -> any
```
## Parameters

<TypeTable
  type={{
    'values': {
      'description': 'The sequence of values from which to extract the minimum. Must not be empty.',
      'type': 'any',
      'default': 'None'
    }
  }}
/>

Determines the maximum of a sequence of values.

<TypstPreview
  code={`
  #calc.max(1, -3, -5, 20, 3, 6) \
  #calc.max("typst", "is", "cool")
`}
  image='/typst-fumadocs/assets/7cbdbb1568e2bb225b7e69185f0e217.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.max(
  values
) -> any
```
## Parameters

<TypeTable
  type={{
    'values': {
      'description': 'The sequence of values from which to extract the maximum. Must not be empty.',
      'type': 'any',
      'default': 'None'
    }
  }}
/>

Determines whether an integer is even.

<TypstPreview
  code={`
  #calc.even(4) \
  #calc.even(5) \
  #range(10).filter(calc.even)
`}
  image='/typst-fumadocs/assets/61517eabdebf59e22801bc307aeaec00.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.even(
  value
) -> bool
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to check for evenness.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Determines whether an integer is odd.

<TypstPreview
  code={`
  #calc.odd(4) \
  #calc.odd(5) \
  #range(10).filter(calc.odd)
`}
  image='/typst-fumadocs/assets/e78c6254542743d14876022717403f8c.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.odd(
  value
) -> bool
```
## Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The number to check for oddness.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates the remainder of two numbers.

The value `calc.rem(x, y){:typst}` always has the same sign as `x{:typst}`, and is smaller in magnitude than `y{:typst}`.

This can error if given a [`decimal{:typst}`](/docs/reference/foundations/decimal/) input and the dividend is too small in magnitude compared to the divisor.

<TypstPreview
  code={`
  #calc.rem(7, 3) \
  #calc.rem(7, -3) \
  #calc.rem(-7, 3) \
  #calc.rem(-7, -3) \
  #calc.rem(1.75, 0.5)
`}
  image='/typst-fumadocs/assets/87d90077c059ff8a9a6549bb59622981.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.rem(
  dividend,
  divisor
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'dividend': {
      'description': 'The dividend of the remainder.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'divisor': {
      'description': 'The divisor of the remainder.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Performs euclidean division of two numbers.

The result of this computation is that of a division rounded to the integer `n{:typst}` such that the dividend is greater than or equal to `n{:typst}` times the divisor.

This can error if the resulting number is larger than the maximum value or smaller than the minimum value for its type.

<TypstPreview
  code={`
  #calc.div-euclid(7, 3) \
  #calc.div-euclid(7, -3) \
  #calc.div-euclid(-7, 3) \
  #calc.div-euclid(-7, -3) \
  #calc.div-euclid(1.75, 0.5) \
  #calc.div-euclid(decimal("1.75"), decimal("0.5"))
`}
  image='/typst-fumadocs/assets/e3de88195be86ab9661116a389316cfe.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.div-euclid(
  dividend,
  divisor
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'dividend': {
      'description': 'The dividend of the division.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'divisor': {
      'description': 'The divisor of the division.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

This calculates the least nonnegative remainder of a division.

Warning: Due to a floating point round-off error, the remainder may equal the absolute value of the divisor if the dividend is much smaller in magnitude than the divisor and the dividend is negative. This only applies for floating point inputs.

In addition, this can error if given a [`decimal{:typst}`](/docs/reference/foundations/decimal/) input and the dividend is too small in magnitude compared to the divisor.

<TypstPreview
  code={`
  #calc.rem-euclid(7, 3) \
  #calc.rem-euclid(7, -3) \
  #calc.rem-euclid(-7, 3) \
  #calc.rem-euclid(-7, -3) \
  #calc.rem-euclid(1.75, 0.5) \
  #calc.rem-euclid(decimal("1.75"), decimal("0.5"))
`}
  image='/typst-fumadocs/assets/cac5f61cb0beadf5800a29f08a061c5a.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.rem-euclid(
  dividend,
  divisor
) -> int float decimal
```
## Parameters

<TypeTable
  type={{
    'dividend': {
      'description': 'The dividend of the remainder.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'divisor': {
      'description': 'The divisor of the remainder.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Calculates the quotient (floored division) of two numbers.

Note that this function will always return an [integer](/docs/reference/foundations/int/), and will error if the resulting number is larger than the maximum 64-bit signed integer or smaller than the minimum for that type.

<TypstPreview
  code={`
  $ "quo"(a, b) &= floor(a/b) \
    "quo"(14, 5) &= #calc.quo(14, 5) \
    "quo"(3.46, 0.5) &= #calc.quo(3.46, 0.5) $
`}
  image='/typst-fumadocs/assets/4848bce8e009c059a3418c08b43db5.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.quo(
  dividend,
  divisor
) -> int
```
## Parameters

<TypeTable
  type={{
    'dividend': {
      'description': 'The dividend of the quotient.',
      'type': 'int | float | decimal',
      'default': 'None'
    },
    'divisor': {
      'description': 'The divisor of the quotient.',
      'type': 'int | float | decimal',
      'default': 'None'
    }
  }}
/>

Calculates the p-norm of a sequence of values.

<TypstPreview
  code={`
  #calc.norm(1, 2, -3, 0.5) \
  #calc.norm(p: 3, 1, 2)
`}
  image='/typst-fumadocs/assets/c838bb6a3d014d601035ea215b5bbe1c.png'
  alt='Preview'
  editable={true}
/>

```typst
#calc.norm(
  p: float,
  values
) -> float
```
## Parameters

<TypeTable
  type={{
    'p': {
      'description': 'The p value to calculate the p-norm of.',
      'type': 'float',
      'default': '<code><span class="typ-num">2.0</span></code>'
    },
    'values': {
      'description': 'The sequence of values from which to calculate the p-norm. Returns `0.0{:typst}` if empty.',
      'type': 'float',
      'default': 'None'
    }
  }}
/>


