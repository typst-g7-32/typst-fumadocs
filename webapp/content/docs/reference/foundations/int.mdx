---
title: "Integer"
description: "Documentation for the Integer type."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

A whole number.

The number can be negative, zero, or positive. As Typst uses 64 bits to store integers, integers cannot be smaller than `-9223372036854775808{:typst}` or larger than `9223372036854775807{:typst}`. Integer literals are always positive, so a negative integer such as `-1{:typst}` is semantically the negation `-{:typst}` of the positive literal `1{:typst}`. A positive integer greater than the maximum value and a negative integer less than or equal to the minimum value cannot be represented as an integer literal, and are instead parsed as a `float{:typst}`. The minimum integer value can still be obtained through integer arithmetic.

The number can also be specified as hexadecimal, octal, or binary by starting it with a zero followed by either `x{:typst}`, `o{:typst}`, or `b{:typst}`.

You can convert a value to an integer with this type's constructor.

## Example

<TypstPreview
  code={`
  #(1 + 2) \
  #(2 - 5) \
  #(3 + 4 < 8)

  #0xff \
  #0o10 \
  #0b1001
`}
  image='/typst-fumadocs/assets/c1fa714490d9acd7860c0dd18c480526'
  alt='Preview'
  editable={true}
/>

## Constructor
Converts a value to an integer. Raises an error if there is an attempt to parse an invalid string or produce an integer that doesn't fit into a 64-bit signed integer.

- Booleans are converted to `0{:typst}` or `1{:typst}`.
- Floats and decimals are rounded to the next 64-bit integer towards zero.
- Strings are parsed in base 10 by default.

<TypstPreview
  code={`
  #int(false) \
  #int(true) \
  #int(2.7) \
  #int(decimal("3.8")) \
  #(int("27") + int("4")) \
  #(int("beef", base: 16))
`}
  image='/typst-fumadocs/assets/c4de2725fb7029ca72848c9f3c01a117'
  alt='Preview'
  editable={true}
/>

```typst
#int(
  value,
  base: int
) -> int
```
### Parameters

<TypeTable
  type={{
    'value': {
      'description': 'The value that should be converted to an integer.',
      'type': 'bool | int | float | str | decimal',
      'default': 'None'
    },
    'base': {
      'description': 'The base (radix) for parsing strings, between 2 and 36.',
      'type': 'int',
      'default': '<code><span class="typ-num">10</span></code>'
    }
  }}
/>


## Methods
Calculates the sign of an integer.

- If the number is positive, returns `1{:typst}`.
- If the number is negative, returns `-1{:typst}`.
- If the number is zero, returns `0{:typst}`.

<TypstPreview
  code={`
  #(5).signum() \
  #(-5).signum() \
  #(0).signum()
`}
  image='/typst-fumadocs/assets/562726d9517a67df2c82335941895a06'
  alt='Preview'
  editable={true}
/>

Calculates the bitwise NOT of an integer.

For the purposes of this function, the operand is treated as a signed integer of 64 bits.

<TypstPreview
  code={`
  #4.bit-not() \
  #(-1).bit-not()
`}
  image='/typst-fumadocs/assets/dc060efa9e84fb3dd52c7e2f3725842a'
  alt='Preview'
  editable={true}
/>

Calculates the bitwise AND between two integers.

For the purposes of this function, the operands are treated as signed integers of 64 bits.

<TypstPreview
  code={`
  #128.bit-and(192)
`}
  image='/typst-fumadocs/assets/927c13ad6f976e3e6ca9b71dcdaedec2'
  alt='Preview'
  editable={true}
/>

```typst
#int.bit-and(
  rhs
) -> int
```
### Parameters

<TypeTable
  type={{
    'rhs': {
      'description': 'The right-hand operand of the bitwise AND.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates the bitwise OR between two integers.

For the purposes of this function, the operands are treated as signed integers of 64 bits.

<TypstPreview
  code={`
  #64.bit-or(32)
`}
  image='/typst-fumadocs/assets/cda54a333b5f8fef1521f6cb5de24550'
  alt='Preview'
  editable={true}
/>

```typst
#int.bit-or(
  rhs
) -> int
```
### Parameters

<TypeTable
  type={{
    'rhs': {
      'description': 'The right-hand operand of the bitwise OR.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Calculates the bitwise XOR between two integers.

For the purposes of this function, the operands are treated as signed integers of 64 bits.

<TypstPreview
  code={`
  #64.bit-xor(96)
`}
  image='/typst-fumadocs/assets/2943eab0e2f921759c1927c0845a4beb'
  alt='Preview'
  editable={true}
/>

```typst
#int.bit-xor(
  rhs
) -> int
```
### Parameters

<TypeTable
  type={{
    'rhs': {
      'description': 'The right-hand operand of the bitwise XOR.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Shifts the operand's bits to the left by the specified amount.

For the purposes of this function, the operand is treated as a signed integer of 64 bits. An error will occur if the result is too large to fit in a 64-bit integer.

<TypstPreview
  code={`
  #33.bit-lshift(2) \
  #(-1).bit-lshift(3)
`}
  image='/typst-fumadocs/assets/9085484b226c6c6a4ade4fdfbb9f4ed8'
  alt='Preview'
  editable={true}
/>

```typst
#int.bit-lshift(
  shift
) -> int
```
### Parameters

<TypeTable
  type={{
    'shift': {
      'description': 'The amount of bits to shift. Must not be negative.',
      'type': 'int',
      'default': 'None'
    }
  }}
/>

Shifts the operand's bits to the right by the specified amount. Performs an arithmetic shift by default (extends the sign bit to the left, such that negative numbers stay negative), but that can be changed by the `logical{:typst}` parameter.

For the purposes of this function, the operand is treated as a signed integer of 64 bits.

<TypstPreview
  code={`
  #64.bit-rshift(2) \
  #(-8).bit-rshift(2) \
  #(-8).bit-rshift(2, logical: true)
`}
  image='/typst-fumadocs/assets/81e6da07e0993b30ed9ef7e30df8ed4e'
  alt='Preview'
  editable={true}
/>

```typst
#int.bit-rshift(
  shift,
  logical: bool
) -> int
```
### Parameters

<TypeTable
  type={{
    'shift': {
      'description': 'The amount of bits to shift. Must not be negative.  Shifts larger than 63 are allowed and will cause the return value to saturate. For non-negative numbers, the return value saturates at `0{:typst}`, while, for negative numbers, it saturates at `-1{:typst}` if `logical{:typst}` is set to `false{:typst}`, or `0{:typst}` if it is `true{:typst}`. This behavior is consistent with just applying this operation multiple times. Therefore, the shift will always succeed.',
      'type': 'int',
      'default': 'None'
    },
    'logical': {
      'description': 'Toggles whether a logical (unsigned) right shift should be performed instead of arithmetic right shift. If this is `true{:typst}`, negative operands will not preserve their sign bit, and bits which appear to the left after the shift will be `0{:typst}`. This parameter has no effect on non-negative operands.',
      'type': 'bool',
      'default': '<code><span class="typ-key">false</span></code>'
    }
  }}
/>

Converts bytes to an integer.

<TypstPreview
  code={`
  #int.from-bytes(bytes((0, 0, 0, 0, 0, 0, 0, 1))) \
  #int.from-bytes(bytes((1, 0, 0, 0, 0, 0, 0, 0)), endian: "big")
`}
  image='/typst-fumadocs/assets/2342cf4345948a2d1fb61703db4728b0'
  alt='Preview'
  editable={true}
/>

```typst
#int.from-bytes(
  bytes,
  endian: str,
  signed: bool
) -> int
```
### Parameters

<TypeTable
  type={{
    'bytes': {
      'description': 'The bytes that should be converted to an integer.  Must be of length at most 8 so that the result fits into a 64-bit signed integer.',
      'type': 'bytes',
      'default': 'None'
    },
    'endian': {
      'description': 'The endianness of the conversion.',
      'type': 'str',
      'default': '<code><span class="typ-str">&quot;little&quot;</span></code>'
    },
    'signed': {
      'description': 'Whether the bytes should be treated as a signed integer. If this is `true{:typst}` and the most significant bit is set, the resulting number will negative.',
      'type': 'bool',
      'default': '<code><span class="typ-key">true</span></code>'
    }
  }}
/>

Converts an integer to bytes.

<TypstPreview
  code={`
  #array(10000.to-bytes(endian: "big")) \
  #array(10000.to-bytes(size: 4))
`}
  image='/typst-fumadocs/assets/145ee0196e1e54e1216232195f2f0122'
  alt='Preview'
  editable={true}
/>

```typst
#int.to-bytes(
  endian: str,
  size: int
) -> bytes
```
### Parameters

<TypeTable
  type={{
    'endian': {
      'description': 'The endianness of the conversion.',
      'type': 'str',
      'default': '<code><span class="typ-str">&quot;little&quot;</span></code>'
    },
    'size': {
      'description': 'The size in bytes of the resulting bytes (must be at least zero). If the integer is too large to fit in the specified size, the conversion will truncate the remaining bytes based on the endianness. To keep the same resulting value, if the endianness is big-endian, the truncation will happen at the rightmost bytes. Otherwise, if the endianness is little-endian, the truncation will happen at the leftmost bytes.  Be aware that if the integer is negative and the size is not enough to make the number fit, when passing the resulting bytes to `int.from-bytes{:typst}`, the resulting number might be positive, as the most significant bit might not be set to 1.',
      'type': 'int',
      'default': '<code><span class="typ-num">8</span></code>'
    }
  }}
/>


