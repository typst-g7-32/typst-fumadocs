---
title: "Left/Right"
description: "Documentation for the lr functions."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Delimiter matching.

The `lr{:typst}` function allows you to match two delimiters and scale them with the content they contain. While this also happens automatically for delimiters that match syntactically, `lr{:typst}` allows you to match two arbitrary delimiters and control their size exactly. Apart from the `lr{:typst}` function, Typst provides a few more functions that create delimiter pairings for absolute, ceiled, and floored values as well as norms.

To prevent a delimiter from being matched by Typst, and thus auto-scaled, escape it with a backslash. To instead disable auto-scaling completely, use `set math.lr(size: 1em){:typst}`.

## Example

<TypstPreview
  code={`
  $ [a, b/2] $
  $ lr(]sum_(x=1)^n], size: #50%) x $
  $ abs((x + y) / 2) $
  $ \{ (x / y) \} $
  #set math.lr(size: 1em)
  $ { (a / b), a, b in (0; 1/2] } $
`}
  image='/typst-fumadocs/assets/63330327318f138d9440594430988bbb'
  alt='Preview'
  editable={true}
/>

Scales delimiters.

While matched delimiters scale by default, this can be used to scale unmatched delimiters and to control the delimiter scaling more precisely.

```typst
#math.lr(
  size: relative,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'size': {
      'description': 'The size of the brackets, relative to the height of the wrapped content.',
      'type': 'relative',
      'default': '<code><span class="typ-num">100%</span> <span class="typ-op">+</span> <span class="typ-num">0pt</span></code>'
    },
    'body': {
      'description': 'The delimited content, including the delimiters.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

Scales delimiters vertically to the nearest surrounding `lr(){:typst}` group.

<TypstPreview
  code={`
  $ { x mid(|) sum_(i=1)^n w_i|f_i (x)| < 1 } $
`}
  image='/typst-fumadocs/assets/a29f9290887cdd1f41b9003f982e3560'
  alt='Preview'
  editable={true}
/>

```typst
#math.mid(
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content to be scaled.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

Takes the absolute value of an expression.

<TypstPreview
  code={`
  $ abs(x/2) $
`}
  image='/typst-fumadocs/assets/5892ee44ad18813000297eeafd1b6e78'
  alt='Preview'
  editable={true}
/>

```typst
#math.abs(
  size: relative,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'size': {
      'description': 'The size of the brackets, relative to the height of the wrapped content.  Default: The current value of [`lr.size{:typst}`](/docs/reference/math/lr/#functions-lr-size).',
      'type': 'relative',
      'default': 'None'
    },
    'body': {
      'description': 'The expression to take the absolute value of.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

Takes the norm of an expression.

<TypstPreview
  code={`
  $ norm(x/2) $
`}
  image='/typst-fumadocs/assets/602e918d9e4207139477dfb451df53cd'
  alt='Preview'
  editable={true}
/>

```typst
#math.norm(
  size: relative,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'size': {
      'description': 'The size of the brackets, relative to the height of the wrapped content.  Default: The current value of [`lr.size{:typst}`](/docs/reference/math/lr/#functions-lr-size).',
      'type': 'relative',
      'default': 'None'
    },
    'body': {
      'description': 'The expression to take the norm of.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

Floors an expression.

<TypstPreview
  code={`
  $ floor(x/2) $
`}
  image='/typst-fumadocs/assets/3c3107954755188561218b3da59b9b88'
  alt='Preview'
  editable={true}
/>

```typst
#math.floor(
  size: relative,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'size': {
      'description': 'The size of the brackets, relative to the height of the wrapped content.  Default: The current value of [`lr.size{:typst}`](/docs/reference/math/lr/#functions-lr-size).',
      'type': 'relative',
      'default': 'None'
    },
    'body': {
      'description': 'The expression to floor.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

Ceils an expression.

<TypstPreview
  code={`
  $ ceil(x/2) $
`}
  image='/typst-fumadocs/assets/f0cd1c0e8d2655688398c799bc806a38'
  alt='Preview'
  editable={true}
/>

```typst
#math.ceil(
  size: relative,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'size': {
      'description': 'The size of the brackets, relative to the height of the wrapped content.  Default: The current value of [`lr.size{:typst}`](/docs/reference/math/lr/#functions-lr-size).',
      'type': 'relative',
      'default': 'None'
    },
    'body': {
      'description': 'The expression to ceil.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

Rounds an expression.

<TypstPreview
  code={`
  $ round(x/2) $
`}
  image='/typst-fumadocs/assets/b45f33012980296a7361d593384f333c'
  alt='Preview'
  editable={true}
/>

```typst
#math.round(
  size: relative,
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'size': {
      'description': 'The size of the brackets, relative to the height of the wrapped content.  Default: The current value of [`lr.size{:typst}`](/docs/reference/math/lr/#functions-lr-size).',
      'type': 'relative',
      'default': 'None'
    },
    'body': {
      'description': 'The expression to round.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>


