---
title: "Under/Over"
description: "Documentation for the underover functions."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Delimiters above or below parts of an equation.

The braces and brackets further allow you to add an optional annotation below or above themselves.

A horizontal line under content.

<TypstPreview
  code={`
  $ underline(1 + 2 + ... + 5) $
`}
  image='/typst-fumadocs/assets/90fbf6ae4b8e62a139c6b4bd8329f2ab.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.underline(
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content above the line.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

A horizontal line over content.

<TypstPreview
  code={`
  $ overline(1 + 2 + ... + 5) $
`}
  image='/typst-fumadocs/assets/6eb6edcdeea961c6dd0c7657a98b57e1.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.overline(
  body
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content below the line.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

A horizontal brace under content, with an optional annotation below.

<TypstPreview
  code={`
  $ underbrace(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/ce5d55602c3ea271c5f9483757b86348.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.underbrace(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content above the brace.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content below the brace.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal brace over content, with an optional annotation above.

<TypstPreview
  code={`
  $ overbrace(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/41739f52a0a2006372edca142286e55a.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.overbrace(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content below the brace.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content above the brace.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal bracket under content, with an optional annotation below.

<TypstPreview
  code={`
  $ underbracket(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/c84735e83bc2757b999a3ad87056973b.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.underbracket(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content above the bracket.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content below the bracket.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal bracket over content, with an optional annotation above.

<TypstPreview
  code={`
  $ overbracket(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/a95cfe4723f0016480ef535731a5ad25.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.overbracket(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content below the bracket.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content above the bracket.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal parenthesis under content, with an optional annotation below.

<TypstPreview
  code={`
  $ underparen(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/4f11d79431033e7812308ab393328912.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.underparen(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content above the parenthesis.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content below the parenthesis.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal parenthesis over content, with an optional annotation above.

<TypstPreview
  code={`
  $ overparen(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/9f7044b8c228c1bf68a090eb72dfaf6a.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.overparen(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content below the parenthesis.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content above the parenthesis.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal tortoise shell bracket under content, with an optional annotation below.

<TypstPreview
  code={`
  $ undershell(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/f440038aa648a4fde405b0138d0c4af6.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.undershell(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content above the tortoise shell bracket.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content below the tortoise shell bracket.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>

A horizontal tortoise shell bracket over content, with an optional annotation above.

<TypstPreview
  code={`
  $ overshell(0 + 1 + dots.c + n, n + 1 "numbers") $
`}
  image='/typst-fumadocs/assets/1355d283aaa9b9fa97ae98fb78ac0657.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.overshell(
  body,
  annotation
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content below the tortoise shell bracket.',
      'type': 'content',
      'default': 'None'
    },
    'annotation': {
      'description': 'The optional content above the tortoise shell bracket.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    }
  }}
/>


