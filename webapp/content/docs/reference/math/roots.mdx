---
title: "Roots"
description: "Documentation for the roots functions."
---
import { TypeTable } from 'fumadocs-ui/components/type-table';

Square and non-square roots.

## Example

```typst
$ sqrt(3 - 2 sqrt(2)) = sqrt(2) - 1 $
$ root(3, x) $
```


## `root`

A general root.

```typst
$ root(3, x) $
```

```#math.root(index, radicand) -> content```

<TypeTable
  type={{
    'index': {
      'description': 'Which root of the radicand to take.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    },
    'radicand': {
      'description': 'The expression to take the root of.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>


## `sqrt`

A square root.

```typst
$ sqrt(3 - 2 sqrt(2)) = sqrt(2) - 1 $
```

```#math.sqrt(radicand) -> content```

<TypeTable
  type={{
    'radicand': {
      'description': 'The expression to take the square root of.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>


