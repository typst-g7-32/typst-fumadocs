---
title: "Roots"
description: "Documentation for the roots functions."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Square and non-square roots.

## Example

<TypstPreview
  code={`
  $ sqrt(3 - 2 sqrt(2)) = sqrt(2) - 1 $
  $ root(3, x) $
`}
  image='/typst-fumadocs/assets/609310fb74b9404b029e8b188a39ef2b'
  alt='Preview'
  editable={true}
/>

A general root.

<TypstPreview
  code={`
  $ root(3, x) $
`}
  image='/typst-fumadocs/assets/e5d701286528c37ac6ad4075120fe08f'
  alt='Preview'
  editable={true}
/>

```typst
#math.root(
  index,
  radicand
) -> content
```
## Parameters

<TypeTable
  type={{
    'index': {
      'description': 'Which root of the radicand to take.',
      'type': 'none | content',
      'default': '<code><span class="typ-key">none</span></code>'
    },
    'radicand': {
      'description': 'The expression to take the root of.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>

A square root.

<TypstPreview
  code={`
  $ sqrt(3 - 2 sqrt(2)) = sqrt(2) - 1 $
`}
  image='/typst-fumadocs/assets/e6d87229d2ccd4badf9b9dc82c95aa69'
  alt='Preview'
  editable={true}
/>

```typst
#math.sqrt(
  radicand
) -> content
```
## Parameters

<TypeTable
  type={{
    'radicand': {
      'description': 'The expression to take the square root of.',
      'type': 'content',
      'default': 'None'
    }
  }}
/>


