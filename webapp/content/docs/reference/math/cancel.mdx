---
title: "Cancel"
description: "Documentation for the `cancel` function."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { TypstPreview } from '@/components/typst/preview';

Displays a diagonal line over a part of an equation.

This is commonly used to show the elimination of a term.

## Example

<TypstPreview
  code={`
  Here, we can simplify:
  $ (a dot b dot cancel(x)) /
      cancel(x) $
`}
  image='/typst-fumadocs/assets/7d5119bd78ca4e4dacdd63bcf2ddcaf0.png'
  alt='Preview'
  editable={true}
/>

```typst
#math.cancel(
  body,
  length: relative,
  inverted: bool,
  cross: bool,
  angle: auto | angle | function,
  stroke: length | color | gradient | stroke | tiling | dictionary
) -> content
```
## Parameters

<TypeTable
  type={{
    'body': {
      'description': 'The content over which the line should be placed.',
      'type': 'content',
      'default': 'None'
    },
    'length': {
      'description': 'The length of the line, relative to the length of the diagonal spanning the whole element being "cancelled". A value of `100%{:typst}` would then have the line span precisely the element\'s diagonal.  <TypstPreview   code={`   $ a + cancel(x, length: #200%)       - cancel(x, length: #200%) $ `}   image=\'/typst-fumadocs/assets/fd148a56b3439c5e7fa4027244c99cac\'   alt=\'Preview\'   editable={true} />',
      'type': 'relative',
      'default': '<code><span class="typ-num">100%</span> <span class="typ-op">+</span> <span class="typ-num">0.3em</span></code>'
    },
    'inverted': {
      'description': 'Whether the cancel line should be inverted (flipped along the y-axis). For the default angle setting, inverted means the cancel line points to the top left instead of top right.  <TypstPreview   code={`   $ (a cancel((b + c), inverted: #true)) /       cancel(b + c, inverted: #true) $ `}   image=\'/typst-fumadocs/assets/19696e45aa5e672f241d0899e5cdd76d\'   alt=\'Preview\'   editable={true} />',
      'type': 'bool',
      'default': '<code><span class="typ-key">false</span></code>'
    },
    'cross': {
      'description': 'Whether two opposing cancel lines should be drawn, forming a cross over the element. Overrides `inverted{:typst}`.  <TypstPreview   code={`   $ cancel(Pi, cross: #true) $ `}   image=\'/typst-fumadocs/assets/6e2222d3d2e291c0e7c1a03459a37025\'   alt=\'Preview\'   editable={true} />',
      'type': 'bool',
      'default': '<code><span class="typ-key">false</span></code>'
    },
    'angle': {
      'description': 'How much to rotate the cancel line.  - If given an angle, the line is rotated by that angle clockwise with respect to the y-axis. - If `auto{:typst}`, the line assumes the default angle; that is, along the rising diagonal of the content box. - If given a function `angle => angle{:typst}`, the line is rotated, with respect to the y-axis, by the angle returned by that function. The function receives the default angle as its input.  <TypstPreview   code={`   $ cancel(Pi)     cancel(Pi, angle: #0deg)     cancel(Pi, angle: #45deg)     cancel(Pi, angle: #90deg)     cancel(1/(1+x), angle: #(a => a + 45deg))     cancel(1/(1+x), angle: #(a => a + 90deg)) $ `}   image=\'/typst-fumadocs/assets/38212630bf4a4126384abbb4ce4dd71b\'   alt=\'Preview\'   editable={true} />',
      'type': 'auto | angle | function',
      'default': '<code><span class="typ-key">auto</span></code>'
    },
    'stroke': {
      'description': 'How to [stroke](/docs/reference/visualize/stroke/) the cancel line.  <TypstPreview   code={`   $ cancel(     sum x,     stroke: #(       paint: red,       thickness: 1.5pt,       dash: "dashed",     ),   ) $ `}   image=\'/typst-fumadocs/assets/28257b7a29918744372f166e763f080c\'   alt=\'Preview\'   editable={true} />',
      'type': 'length | color | gradient | stroke | tiling | dictionary',
      'default': '<code><span class="typ-num">0.05em</span></code>'
    }
  }}
/>


